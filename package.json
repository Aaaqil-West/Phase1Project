{
  "name": "free-to-play-games",
  "version": "1.0.0",
  "description": "A web app for browsing free-to-play games",
  "main": "index.html",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}